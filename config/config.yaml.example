# Sentinel Configuration
# Copy this file to config.yaml and add your credentials

# This file contains credentials for automated data download.
# If you don't have credentials, you can still use:
#   - Demo mode: python scripts/analyze_city.py --demo
#   - Manual download from: https://browser.dataspace.copernicus.eu

# Copernicus Data Space Credentials
# ---------------------------------
# Required ONLY for automated download via download_products.py
# Get credentials at: https://dataspace.copernicus.eu
#   1. Register/Login
#   2. User Settings > OAuth clients
#   3. Create new client

sentinel:
  client_id: null      # Your OAuth client ID
  client_secret: null  # Your OAuth client secret
  max_cloud_cover: 20  # Maximum cloud cover % for searches